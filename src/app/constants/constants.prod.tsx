import { environment } from '../../environments/environment';

// URLs
export const fetchUsersUrl = environment.apiUrl + 'users';

// json-server
export const SHOW_JSON_SERVER_API = false;
export const JSON_SERVER_API_URL = '';
